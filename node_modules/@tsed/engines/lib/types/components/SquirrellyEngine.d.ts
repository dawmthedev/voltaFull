import { Engine, EngineOptions } from "./Engine.js";
export declare class SquirrellyEngine extends Engine {
    protected $compile(template: string, options: EngineOptions): (options: EngineOptions) => any;
}
