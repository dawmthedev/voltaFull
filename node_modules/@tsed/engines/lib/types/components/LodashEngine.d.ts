import { Engine } from "./Engine.js";
export declare class LodashEngine extends Engine {
    render(template: string, options: any): any;
    protected $compile(template: string, options: any): any;
}
