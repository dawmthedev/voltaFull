import { Engine } from "./Engine.js";
export declare class HandlebarsEngine extends Engine {
    protected configure(options: any): void;
    protected $compile(template: string, options: any): any;
}
