import { ServerException } from "../core/ServerException.js";
export declare class InternalServerError extends ServerException {
    static readonly STATUS = 500;
    constructor(message: string, origin?: Error | string | any);
}
