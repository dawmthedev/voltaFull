import { isClass, isString, nameOf, Store } from "@tsed/core";
import { MONGOOSE_MODEL_NAME } from "../constants/constants.js";
/**
 * @ignore
 */
export function resolveRefType(type) {
    if (isString(type)) {
        return type;
    }
    if (!isClass(type)) {
        type = type();
    }
    return Store.from(type).get(MONGOOSE_MODEL_NAME) || nameOf(type);
}
//# sourceMappingURL=resolveRefType.js.map