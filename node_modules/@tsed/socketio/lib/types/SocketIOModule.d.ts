/// <reference types="node" />
/// <reference types="node" />
import { AfterListen, InjectorService, OnDestroy, Provider } from "@tsed/common";
import { ServerOptions } from "socket.io";
import Http from "http";
import Https from "https";
/**
 * @ignore
 */
export declare class SocketIOModule implements AfterListen, OnDestroy {
    protected disableRoutesSummary: boolean;
    protected settings: Partial<ServerOptions>;
    protected injector: InjectorService;
    protected httpServer: Http.Server | null;
    protected httpsServer: Https.Server | null;
    private io;
    private socketIOService;
    $afterListen(): Promise<void>;
    $onDestroy(): Promise<unknown>;
    /**
     *
     * @returns {Provider<any>[]}
     */
    protected getWebsocketServices(): Provider<any>[];
    /**
     *
     */
    protected printSocketEvents(): void;
}
