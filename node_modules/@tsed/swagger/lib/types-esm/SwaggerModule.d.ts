import { Configuration, InjectorService, OnReady, OnRoutesInit, PlatformApplication } from "@tsed/common";
import { Env } from "@tsed/core";
import { SwaggerSettings } from "./interfaces/SwaggerSettings.js";
import { SwaggerService } from "./services/SwaggerService.js";
/**
 * @ignore
 */
export declare class SwaggerModule implements OnRoutesInit, OnReady {
    injector: InjectorService;
    app: PlatformApplication;
    configuration: Configuration;
    swaggerService: SwaggerService;
    env: Env;
    disableRoutesSummary: boolean;
    private loaded;
    get settings(): SwaggerSettings[];
    /**
     *
     */
    $onRoutesInit(): void;
    $onReady(): void;
    generateSpecFiles(): Promise<void[]>;
    private getUrls;
    /**
     *
     * @param conf
     * @param urls
     */
    private createRouter;
    private middlewareSwaggerJson;
}
