import { Store } from "@tsed/core";
import { INJECTABLE_PROP } from "@tsed/di";
export function getInterceptorOptions(target, propertyKey) {
    return Store.from(target).get(INJECTABLE_PROP)?.[propertyKey]?.options;
}
//# sourceMappingURL=getInterceptorOptions.js.map