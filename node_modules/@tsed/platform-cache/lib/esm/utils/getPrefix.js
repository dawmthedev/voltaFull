import { nameOf } from "@tsed/core";
import { getInterceptorOptions } from "./getInterceptorOptions.js";
export function getPrefix(target, propertyKey) {
    const { prefix } = getInterceptorOptions(target, propertyKey);
    if (prefix) {
        return [prefix];
    }
    return [nameOf(target), propertyKey];
}
//# sourceMappingURL=getPrefix.js.map