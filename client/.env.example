# Example environment file for the React client
#
# REACT_APP_STAGE controls which API base URL is used.
# Valid values: local, development, production
# See src/apiConfig.ts for the mapping.
REACT_APP_STAGE=development
