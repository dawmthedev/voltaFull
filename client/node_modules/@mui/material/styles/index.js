const React = require('react');
module.exports = {
  styled: (tag) => (styles) => React.forwardRef((props, ref) => React.createElement(tag, { ref, style: typeof styles === 'function' ? styles({ theme: {} }) : styles, ...props }, props.children)),
  alpha: (color, opacity) => color,
  useTheme: () => ({}),
};
